## Flask base application

Base app with basic CRUD operation and simple user authentication process (account creation).

built with:
* flask;
* flask-auth;
* sqlite for the db;

Based on the official Flask tutorial, can be used for a simple blog or as a base for any other (simple) app.